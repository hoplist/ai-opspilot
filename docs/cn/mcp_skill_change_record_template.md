# MCP / Skill 变更记录模板

本文用于严格记录每次让 Codex 修改 MCP、Skill、backend、RCA 页面或相关文档时发生了什么。

建议每次变更都复制本模板，另存为：

```text
docs/cn/change_records/YYYY-MM-DD-<short-topic>.md
```

## 1. 基本信息

- 日期：
- 操作人：
- 需求来源：
- 关联对话摘要：
- 变更主题：
- 影响范围：

## 2. 需求原文

记录用户原始需求或尽量贴近原话的摘要。

```text
在这里记录需求原文。
```

## 3. 目标

本次变更要达成什么。

- 目标 1：
- 目标 2：
- 目标 3：

## 4. 安全边界

本次变更是否涉及敏感动作。

- 是否允许 MCP 直接操作服务器：否
- 是否允许 MCP 修改 Kubernetes 资源：否
- 是否允许 MCP 修改基础设施配置：否
- 是否允许生成 investigation 记录：是 / 否
- 是否涉及凭证变更：是 / 否
- 是否涉及远端部署：是 / 否

说明：

```text
这里写清楚边界和风险。
```

## 5. 修改文件

记录所有被修改或新增的文件。

| 类型 | 文件 | 说明 |
| --- | --- | --- |
| 新增 / 修改 | `path/to/file` | 做了什么 |

## 6. 新增或修改的 MCP 工具

| 工具名 | 类型 | 入参 | 数据源 | 是否只读 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `tool_name` | 新增 / 修改 | `namespace`, `pod` | logs/events/prometheus | 是 | 说明 |

## 7. 新增或修改的 Skill 能力

记录 Skill 的触发场景、工作流、输出要求。

- 触发场景：
- 推荐工作流：
- 输出格式：
- 回退方式：

## 8. 后端 API 变化

如果后端 API 有变化，记录如下。

| 方法 | 路径 | 类型 | 是否只读 | 说明 |
| --- | --- | --- | --- | --- |
| `GET` | `/api/example` | 新增 / 修改 | 是 | 说明 |

## 9. 部署动作

记录是否同步到了远端、是否重启服务。

- 是否同步到 NFS：
- NFS 路径：
- 是否重启 Deployment：
- Deployment：
- Namespace：
- 备份文件：

命令或动作摘要：

```powershell
在这里记录关键命令。
```

## 10. 验证记录

记录验证命令和结果。

| 验证项 | 命令 / 方法 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 语法检查 | `python -m py_compile ...` | 通过 | 通过 |
| MCP tools/list | JSON-RPC `tools/list` | 出现新工具 | 通过 |
| 真实调用 | JSON-RPC `tools/call` | 返回结构化结果 | 通过 |

## 11. 回滚方式

记录如何回滚。

- 本地文件回滚：
- 远端 NFS 备份文件：
- Deployment 回滚方式：

```powershell
在这里记录回滚命令。
```

## 12. 已知限制

- 限制 1：
- 限制 2：
- 限制 3：

## 13. 后续建议

- 建议 1：
- 建议 2：
- 建议 3：
