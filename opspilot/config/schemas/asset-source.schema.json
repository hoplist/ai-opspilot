{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OpsPilot AssetSource",
  "type": "object",
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": { "const": "opspilot.io/v1" },
    "kind": { "const": "AssetSource" },
    "metadata": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "spec": {
      "type": "object",
      "required": ["kind"],
      "properties": {
        "kind": { "enum": ["jumpserver", "jms", "cmdb", "prometheus", "agent", "manual", "vm"] },
        "region": { "type": "string" },
        "network_zone": { "type": "string" },
        "url": { "type": "string" },
        "credential_ref": { "type": "string" },
        "enabled": { "type": "boolean" },
        "required": { "type": "boolean" },
        "coverage": { "enum": ["complete", "partial", "unknown"] },
        "timeout": { "type": "string" },
        "on_error": { "enum": ["warn", "skip", "error"] },
        "sync": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "mode": { "enum": ["readonly"] },
            "delete_policy": { "enum": ["mark_stale"] },
            "interval": { "type": "string" }
          }
        },
        "note": { "type": "string" }
      }
    }
  }
}
