credentials:
  - name: guangzhou-inner-es
    type: elasticsearch
    environment: test
    username: elastic
    password: "replace-me"
    used_by:
      - datasource:guangzhou-inner-es
    owner: ops
    note: "内网测试环境可由 GitLab 私有仓库明文维护；OpsPilot API 默认只显示 password_set。"
