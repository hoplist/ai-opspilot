apiVersion: v1
kind: LimitRange
metadata:
  name: opspilot-defaults
  namespace: opspilot
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 50m
        memory: 64Mi
      default:
        cpu: 500m
        memory: 256Mi
