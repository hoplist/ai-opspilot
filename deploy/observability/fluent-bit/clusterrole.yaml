apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: fluent-bit-observability
rules:
- apiGroups: [""]
  resources:
  - pods
  - namespaces
  - events
  verbs: ["get", "list", "watch"]

