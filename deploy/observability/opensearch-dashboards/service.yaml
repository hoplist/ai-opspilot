apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards
  namespace: observability
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: opensearch-dashboards
  ports:
  - name: http
    port: 5601
    targetPort: 5601
    nodePort: 32091

