apiVersion: v1
kind: Service
metadata:
  name: opspilot-core
  namespace: opspilot
  labels:
    app.kubernetes.io/name: opspilot-core
    app.kubernetes.io/part-of: opspilot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: opspilot-core
  ports:
    - name: http
      port: 18080
      targetPort: http
