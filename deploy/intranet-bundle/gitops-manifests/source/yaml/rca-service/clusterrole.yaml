apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: auto-inspection-rca-read
rules:
- apiGroups: [""]
  resources:
  - pods
  - pods/log
  - services
  - events
  - namespaces
  - nodes
  verbs: ["get", "list", "watch"]
